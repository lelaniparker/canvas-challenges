import { red } from "ansi-colors"

const family = "Arial, Helvetica, sans-serif"

const divStyle = {
	padding: "1em",
	fontFamily: family
}

const imgStyle = {
	height: "200px"
}

// My styling

const divStyleMessage = {
	fontSize: "3em"
}

export const styles = {
	divStyle,
	imgStyle,
	divStyleMessage
}
